# Masked Sample Data

This folder is allowed to contain masked or synthetic sample data only.

Rules:
- No real customer name
- No real PIC name if sensitive
- No real invoice number
- No credentials
- No database export containing real operational data
- Column mapping must remain identical to the real project structure